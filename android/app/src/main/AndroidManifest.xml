<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 9 及以下写入公共 Download 目录需要 WRITE_EXTERNAL_STORAGE；
         Android 10+ 通过 MediaStore.Downloads 写入，不需要任何存储权限，
         所以 maxSdkVersion=28，Android 13+ 自动忽略此声明，用户侧也不弹权限窗。 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 允许直接弹系统「忽略电池优化」请求弹窗（normal 权限，自动授予，不弹窗询问用户）。
         没有这个权限时，"一键加白"按钮会降级为跳转到应用详情页手动操作。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 悬浮窗 / 系统级覆盖层：用于跨 App 显示计时悬浮胶囊按钮（需用户手动授权，不是运行时弹窗权限） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务（悬浮窗必须依附前台服务才不容易被杀，Android 8+ 强制要求） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14 (API 34) 新增：如果是 SPECIAL_USE 类型的前台服务，需要声明 subtype；
         我们的悬浮计时窗即属于此类，自动授予，不会向用户弹窗。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <!-- 深链：邮件验证/密码重置回跳到 App（com.selfdefense.app://auth/callback） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.selfdefense.app"
                    android:host="auth" />
            </intent-filter>
        </activity>

        <!-- 全局悬浮计时窗：前台服务（specialUse 类型，API 34+ 要求声明 subtype） -->
        <service
            android:name=".FloatingTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating timer overlay for a user-started training session" />
        </service>

        <!-- 蘑菇战士图标（默认启用） -->
        <activity-alias
            android:name=".MushroomAlias"
            android:enabled="true"
            android:icon="@mipmap/ic_mushroom_launcher"
            android:roundIcon="@mipmap/ic_mushroom_launcher_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- 鲍鱼战士图标（默认禁用） -->
        <activity-alias
            android:name=".AbaloneAlias"
            android:enabled="false"
            android:icon="@mipmap/ic_abalone_launcher"
            android:roundIcon="@mipmap/ic_abalone_launcher_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- 最早的默认图标（默认禁用，需在设置里切换） -->
        <activity-alias
            android:name=".DefaultAlias"
            android:enabled="false"
            android:icon="@mipmap/ic_classic_launcher"
            android:roundIcon="@mipmap/ic_classic_launcher"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
